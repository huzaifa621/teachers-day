// Fixed tribute styling — students no longer choose font/color/size, so this
// is the single source of truth. Keep in sync with the frontend's copy in
// frontend/components/shared.js (FIXED_STYLE).
module.exports = {
  FONT_FAMILY: 'Georgia, serif',
  TEXT_COLOR: '#2c1810',
  FONT_SIZE: '18px'
};
